# Components
letter:
    {
        audio (file)
        image (file)
        lettername (string)
        vowel? (bool)
        first? (bool)
        last? (bool)
    }

quiz:
    {
        
    }

# Packages
Text to speech
Media player?

